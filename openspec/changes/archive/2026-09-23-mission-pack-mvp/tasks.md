# Tasks

## 1. 骨格とミッション定義

- [x] 1.1 `lib/ui/home`・`lib/ui/play`・`lib/data/repositories`・共有 models のディレクトリ／空ファイル骨組みを作り、`flutter analyze` がエラーなく通ることを確認する
- [x] 1.2 `Mission`／`MissionRepository`（定数可）を追加し、`missionId=m01` で 10×20・`clearLines`・`goalLines=4`・`bagCount=1`・軽ゴミ1パターン・`dropIntervalMs≈900` を返すことをユニットテストで確認する
- [x] 1.3 カウンタ雛形をやめ `MaterialApp` の初期ルートを Home にし、起動して Home が表示されることを Widget テストまたは手動起動で確認する

## 2. ゲームルールエンジン（UI 非依存）

- [x] 2.1 盤面・テトロミノ7種・有限バッグ（7種シャッフル1周）・スポーンを実装し、バッグ消費と枯渇をユニットテストで確認する
- [x] 2.2 左右移動・簡易回転（基本4向き＋簡易キック）・ソフト／ハードドロップ・ロックを実装し、衝突時に移動／回転が拒否されることをユニットテストで確認する
- [x] 2.3 ライン消去と累計 `linesCleared`、および `linesCleared >= 4` でクリア状態になることをユニットテストで確認する
- [x] 2.4 スポーン不能およびバッグ空かつ未クリアで失敗状態になることをユニットテストで確認する
- [x] 2.5 ミッション開始時に軽ゴミ（1セル以上の初期固定ブロック）が載ることをユニットテストで確認する

## 3. Play ループと画面

- [x] 3.1 `PlayViewModel` に Ticker／定期更新による固定落下と入力 API を接続し、dispose でループが止まることをテストまたはログで確認する
- [x] 3.2 `PlayView` で盤面（CustomPainter または Grid）・Next 1・操作 UI（左右／回転／ソフト／ハード）を表示し、操作がエンジンに伝わることを Widget テストまたは手動で確認する
- [x] 3.3 クリア／失敗時にループを止め、Play 上オーバーレイで「もう一度」「ホームへ」を出し、再挑戦が同 `missionId` で初期化されることを確認する

## 4. Home 遷移と仕上げ

- [x] 4.1 `HomeView`／`HomeViewModel` の開始操作で `missionId` 付き Play へ遷移することを Widget テストまたは手動で確認する
- [x] 4.2 オーバーレイ「ホームへ」で Home に戻ることを確認する
- [x] 4.3 `flutter test` と `flutter analyze` を通し、Flame 未導入・スコア永続化なしであることを依存関係とコードパスで確認する
